(let ((unread-command-events (list '(mouse-1) 97)))
  (progn
    (read-key-sequence "")
    unread-command-events))

(let ((unread-command-events (list '(mouse-1) 'foo)))
  (progn
    (read-key-sequence "")
    unread-command-events))

(let ((unread-command-events (list '(mouse-1) 98 'foo)))
  (progn
    (read-key-sequence "")
    unread-command-events))

(let ((unread-command-events (list '(mouse-1) 98 'foo 'bar)))
  (progn
    (read-key-sequence "")
    unread-command-events))

(let ((unread-command-events (list '(mouse-1) 98 'foo 'bar 'baz)))
  (progn
    (read-key-sequence "")
    unread-command-events))

(let ((unread-command-events '((mouse-1) . 97)))
  (progn
    (read-key-sequence "")
    unread-command-events))

(let ((unread-command-events '((mouse-1) . foo)))
  (progn
    (read-key-sequence "")
    unread-command-events))
