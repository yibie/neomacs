(and (consp (key-binding ""))
     (keymapp (car (key-binding ""))))
(key-binding [])
(keymapp (global-key-binding ""))
(keymapp (global-key-binding []))
(let ((m (make-sparse-keymap)))
  (use-local-map m)
  (let ((maps (key-binding "")))
    (and (consp maps)
         (keymapp (car maps))
         (consp (cdr maps))
         (keymapp (car (cdr maps))))))
(let ((m (make-sparse-keymap)))
  (use-local-map m)
  (key-binding []))
