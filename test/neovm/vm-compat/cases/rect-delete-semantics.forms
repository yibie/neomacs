(with-temp-buffer
  (insert "abcdef")
  (newline)
  (insert "123456")
  (newline)
  (list (delete-rectangle 1 9) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abcdef")
  (newline)
  (insert "123456")
  (newline)
  (list (delete-rectangle 8 7) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abcdef")
  (list (delete-rectangle 20 1) (string-to-list (buffer-string))))
(condition-case err (delete-rectangle "x" 2) (error (car err)))
(condition-case err (delete-rectangle 1 "x") (error (car err)))
