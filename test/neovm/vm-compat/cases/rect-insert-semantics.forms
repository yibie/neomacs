(with-temp-buffer
  (insert "abc")
  (newline)
  (insert "def")
  (newline)
  (goto-char 1)
  (insert-rectangle '("X" "Y"))
  (list (point) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abc")
  (newline)
  (insert "def")
  (newline)
  (goto-char 2)
  (insert-rectangle '("X" "Y"))
  (list (point) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abc")
  (goto-char 2)
  (insert-rectangle '("X" "Y" "Z"))
  (list (point) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abc")
  (goto-char 2)
  (insert-rectangle nil)
  (list (point) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abc")
  (goto-char 2)
  (insert-rectangle 'nil)
  (list (point) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abc")
  (goto-char 2)
  (insert-rectangle '("" ""))
  (list (point) (string-to-list (buffer-string))))
(condition-case err (insert-rectangle 1) (error (car err)))
(condition-case err (insert-rectangle '("a" 1)) (error (car err)))
