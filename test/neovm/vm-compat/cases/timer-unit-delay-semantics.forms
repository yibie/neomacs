(let ((tm (run-at-time "2 min" nil #'ignore)))
  (prog1 (timerp tm)
    (cancel-timer tm)))

(let ((tm (run-at-time "1.5 hour" nil #'ignore)))
  (prog1 (timerp tm)
    (cancel-timer tm)))

(let ((tm (run-with-timer "1 week" nil #'ignore)))
  (prog1 (timerp tm)
    (cancel-timer tm)))

(let ((tm (run-at-time "2 fortnights" nil #'ignore)))
  (prog1 (timerp tm)
    (cancel-timer tm)))

(let ((tm (run-at-time "2min" nil #'ignore)))
  (prog1 (timerp tm)
    (cancel-timer tm)))

(let ((tm (run-at-time "2mins" nil #'ignore)))
  (prog1 (timerp tm)
    (cancel-timer tm)))

(let ((tm (run-at-time "2week" nil #'ignore)))
  (prog1 (timerp tm)
    (cancel-timer tm)))

(let ((tm (run-at-time "2weeks" nil #'ignore)))
  (prog1 (timerp tm)
    (cancel-timer tm)))

(let ((tm (run-at-time "2day" nil #'ignore)))
  (prog1 (timerp tm)
    (cancel-timer tm)))

(let ((tm (run-at-time "2days" nil #'ignore)))
  (prog1 (timerp tm)
    (cancel-timer tm)))

(let ((tm (run-at-time "2month" nil #'ignore)))
  (prog1 (timerp tm)
    (cancel-timer tm)))

(let ((tm (run-at-time "2months" nil #'ignore)))
  (prog1 (timerp tm)
    (cancel-timer tm)))

(let ((tm (run-at-time "2year" nil #'ignore)))
  (prog1 (timerp tm)
    (cancel-timer tm)))

(let ((tm (run-at-time "2years" nil #'ignore)))
  (prog1 (timerp tm)
    (cancel-timer tm)))

(let ((tm (run-at-time "2.5day" nil #'ignore)))
  (prog1 (timerp tm)
    (cancel-timer tm)))

(let ((tm (run-at-time "2.5 day" nil #'ignore)))
  (prog1 (timerp tm)
    (cancel-timer tm)))

(let ((tm (run-at-time "+2day" nil #'ignore)))
  (prog1 (timerp tm)
    (cancel-timer tm)))

(let ((tm (run-at-time "+2 day" nil #'ignore)))
  (prog1 (timerp tm)
    (cancel-timer tm)))
