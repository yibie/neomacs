(subr-arity (symbol-function 'downcase-region))
(subr-arity (symbol-function 'downcase-word))
(subr-arity (symbol-function 'move-end-of-line))
(subr-arity (symbol-function 'just-one-space))
(subr-arity (symbol-function 'kill-append))
(subr-arity (symbol-function 'kill-buffer))
(subr-arity (symbol-function 'kill-line))
(subr-arity (symbol-function 'kill-local-variable))
(subr-arity (symbol-function 'kill-new))
(subr-arity (symbol-function 'kill-region))
(subr-arity (symbol-function 'kill-ring-save))
(subr-arity (symbol-function 'kill-whole-line))
(subr-arity (symbol-function 'kill-word))
