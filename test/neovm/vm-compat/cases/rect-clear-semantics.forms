(with-temp-buffer
  (insert "abcdef")
  (newline)
  (insert "123456")
  (newline)
  (list (clear-rectangle 1 9) (point) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abcdef")
  (newline)
  (insert "123456")
  (newline)
  (list (clear-rectangle 2 10) (point) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abcdef")
  (newline)
  (insert "123456")
  (newline)
  (list (clear-rectangle 8 7) (point) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abc")
  (list (clear-rectangle 20 1) (point) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abc")
  (list (clear-rectangle 1 2 42) (point) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abc")
  (list (clear-rectangle 1 2 "x") (point) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abc")
  (list (clear-rectangle 1 2 1.5) (point) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abc")
  (goto-char 1)
  (list (clear-rectangle 2 2) (point) (string-to-list (buffer-string))))
(condition-case err (clear-rectangle "x" 2) (error (car err)))
(condition-case err (clear-rectangle 1 "x") (error (car err)))
