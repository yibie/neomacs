1	(let ((tm (run-at-time 1 nil #'ignore))) (prog1 (timerp tm) (cancel-timer tm)))
2	(let ((tm (run-at-time 1.5 nil #'ignore))) (prog1 (timerp tm) (cancel-timer tm)))
3	(let ((tm (run-at-time -1 nil #'ignore))) (prog1 (timerp tm) (cancel-timer tm)))
4	(let ((tm (run-at-time ?\a nil #'ignore))) (prog1 (timerp tm) (cancel-timer tm)))
5	(let ((tm (run-with-timer 2 nil #'ignore))) (prog1 (timerp tm) (cancel-timer tm)))
6	(let ((tm (run-with-timer 2.5 nil #'ignore))) (prog1 (timerp tm) (cancel-timer tm)))
7	(let ((tm (run-with-timer 2 0.5 #'ignore))) (prog1 (timerp tm) (cancel-timer tm)))
8	(let ((tm (run-at-time 1 1.5 #'ignore))) (prog1 (timerp tm) (cancel-timer tm)))
9	(let ((tm (run-with-timer 1.25 0.25 #'ignore))) (prog1 (timerp tm) (cancel-timer tm)))
10	(let ((tm (run-at-time 0.0 nil #'ignore))) (prog1 (timerp tm) (cancel-timer tm)))
