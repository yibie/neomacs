(condition-case err
    (progn
      (setq unread-command-events (list 1.0 97))
      (read-char nil nil 0))
  (error (list (car err) unread-command-events)))

(condition-case err
    (progn
      (setq unread-command-events (list 1.0 98 97))
      (read-char nil nil 0))
  (error (list (car err) unread-command-events)))

(condition-case err
    (progn
      (setq unread-command-events (list 1.0 98 97 96))
      (read-char nil nil 0))
  (error (list (car err) unread-command-events)))

(condition-case err
    (progn
      (setq unread-command-events (list 1.0 98 97 96 95))
      (read-char nil nil 0))
  (error (list (car err) unread-command-events)))

(condition-case err
    (progn
      (setq unread-command-events (list 1.0 98 97 96 95 94))
      (read-char nil nil 0))
  (error (list (car err) unread-command-events)))

(condition-case err
    (progn
      (setq unread-command-events '(1.0 . 97))
      (read-char nil nil 0))
  (error (list (car err) unread-command-events)))
