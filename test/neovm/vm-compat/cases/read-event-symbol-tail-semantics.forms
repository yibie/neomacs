(let ((unread-command-events (list 97 'foo)))
  (progn
    (read-event nil nil 0)
    unread-command-events))

(let ((unread-command-events (list 97 98 'foo)))
  (progn
    (read-event nil nil 0)
    unread-command-events))

(let ((unread-command-events (list 97 98 'foo 'bar)))
  (progn
    (read-event nil nil 0)
    unread-command-events))

(let ((unread-command-events (list 97 98 'foo 'bar 'baz)))
  (progn
    (read-event nil nil 0)
    unread-command-events))

(let ((unread-command-events (list 97 98 'foo 'bar 'baz 'qux)))
  (progn
    (read-event nil nil 0)
    unread-command-events))

(let ((unread-command-events (list 97 98 'foo 'bar 'baz 'qux 'quux)))
  (progn
    (read-event nil nil 0)
    unread-command-events))

(let ((unread-command-events (list 97 98 'foo 'bar 'baz 'qux 'quux 'quuz)))
  (progn
    (read-event nil nil 0)
    unread-command-events))

(let ((unread-command-events '(97 . foo)))
  (progn
    (read-event nil nil 0)
    unread-command-events))

(let ((unread-command-events '(foo . 97)))
  (progn
    (read-event nil nil 0)
    unread-command-events))
